# 贡献指南 (Contributing Guide)

感谢您对 HomeAgent 的关注与贡献！为了保持代码质量、系统安全与架构清晰，请在提交代码前仔细阅读本指南。

## 1. 代码规范与架构边界

- **模块隔离**：严禁直接依赖其他业务模块的 `internal` 或 `infrastructure` 实现细节；
- **配置与数据安全**：严禁在代码、注释、提交历史或测试用例中提交任何真实密钥、Token、密码、个人家庭网络 IPv6 地址或设备指纹；
- **示例与测试网段**：IPv6 示例统一使用 RFC 3849 规定的 `2001:db8::/32` 文档网段；IPv4 使用 RFC 5737 文档网段（`192.0.2.0/24`、`198.51.100.0/24`、`203.0.113.0/24`）；MAC 地址使用本地管理单播地址（如 `02:xx:xx:...`）。

## 2. 开发与提交流程

1. **Fork 与创建分支**：
   - 基于主分支拉取特性分支：`git checkout -b feat/your-feature-name`；
2. **测试先行 (TDD)**：
   - 所有的功能增加或行为修改必须包含对应的自动化测试用例；
   - 包含正常成功路径、边界条件以及非法输入的负例拒绝断言；
3. **本地质量门禁验证**：
   - 运行全量竞态检测：`go test -race ./...`
   - 检查 Diff Coverage（不低于 60%）：`./scripts/check-diff-coverage.sh HEAD 60`
   - 检查代码格式与行尾规范：`git diff --check`
4. **提交身份**：
   - 推荐使用 GitHub 隐私保护邮箱（`username@users.noreply.github.com`）作为提交者身份，避免暴露本机用户名或设备主机名。

## 3. Pull Request 要求

- PR 标题应遵循 Conventional Commits 规范（如 `feat: ...`、`fix: ...`、`docs: ...`）；
- 详细说明修改背景、影响范围以及本地验证结果；
- 确保所有 CI 质量流水线检查通过。
